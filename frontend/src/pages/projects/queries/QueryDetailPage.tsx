import React from 'react';
import { Typography } from '@mui/material';

export const QueryDetailPage: React.FC = () => {
  return (
    <div>
      <Typography variant="h4">Query Detail</Typography>
      <Typography variant="body1">Query detail content goes here</Typography>
    </div>
  );
};
