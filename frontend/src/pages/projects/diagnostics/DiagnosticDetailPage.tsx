import React from 'react';
import { Typography } from '@mui/material';

export const DiagnosticDetailPage: React.FC = () => {
  return (
    <div>
      <Typography variant="h4">Diagnostic Detail</Typography>
      <Typography variant="body1">Diagnostic detail content goes here</Typography>
    </div>
  );
};
