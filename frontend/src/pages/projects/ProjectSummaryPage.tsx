import React from 'react';
import { Typography } from '@mui/material';

export const ProjectSummaryPage: React.FC = () => {
  return (
    <div>
      <Typography variant="h4">Project Summary</Typography>
      <Typography variant="body1">Project summary content goes here</Typography>
    </div>
  );
};
